{% render 'layouts/base.liquid' %}
{% block content %}
    <main>
        <article>
            <section class="hero">
                <img src="https://fdnd-agency.directus.app/assets/{{ instruments.photo.id }}" alt="{{ instruments.name }}">
            </section>
           <h1>{{ instruments.name }}</h1>
        </article>
    </main>
{% endblock %}